#include <fstream>
#include <iostream>

using namespace std;

int main(int argc, char* argv[]) {
  // TODO: read the unit test file to understand what should be done here. You
  // can also simply run the unit tests and study the output for guidance.
  //
  // Hint: you will need to output a certain text string to standard output.
  // This should be a single line of code. It is _not_ a return statement.
  //
  // The homework assignments typically will not run unit tests against your
  // application. This is just to show you how that could be done if you want to
  // write your own tests against your app.

  cout << "Assignment 00 Success! Your environment configuration is all clear" << endl;

  return 0;
}